@using EntityExtracterTool.Web.Models
@using PagedList
@using PagedList.Mvc

@model IPagedList<Entity>

@{
    ViewBag.Title = "RemovedEntities";
}

<h2 class="text-center">Entity Information</h2>

<div class="text-center">
    @Html.PagedListPager(Model, page => Url.Action("RemovedEntities", new { page }))
</div>

<div class="row">
    <div class="col-md-12">
        <h3>Removed Entities</h3>
        <table class="table table-bordered">
            <tr>
                <th class="text-center">Entity Key</th>
                <th class="text-center">Entity Value</th>
                <th class="text-center">Entity Description</th>
                <th class="text-center">Entity Last Modified</th>
            </tr>
            @foreach (var entity in Model)
            {
                <tr>
                    <td>
                        @entity.Key
                    </td>
                    <td>
                        @entity.Value
                    </td>
                    <td>
                        @entity.Description
                    </td>
                    <td>
                        @entity.LastModified
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
